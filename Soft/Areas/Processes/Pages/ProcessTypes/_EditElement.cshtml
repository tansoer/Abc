@using Abc.Pages.Common.Extensions
@using Abc.Pages.Processes
@using Abc.Soft.Pages.Components
@using Abc.Facade.Processes
@model Abc.Pages.Processes.ProcessTypesPage

<body>
    <component>@(await Html.RenderComponentAsync<DynamicInputCollection<ThreadTypeView>>
        (RenderMode.Server, new {
        BoundPropertyListName = Html.NameFor(x => x.ThreadTypes), 
        BoundPropertyList = Model.ThreadTypes,
        InputFields = Model.ThreadTypesInputs,
        Title = ProcessTitles.ThreadTypes
    }))</component>
</body>